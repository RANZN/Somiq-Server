# ==============================================================================
# Somiq Server - Environment Configuration Template
# Copy this file to .env.local for local development.
# DO NOT commit .env.local or production secrets to version control.
# ==============================================================================

# Application Environment (LOCAL or PRODUCTION)
APP_ENV=LOCAL
PORT=8080

# JWT Configuration (Secret MUST be at least 32 characters long for HMAC-SHA256)
JWT_SECRET=replace-with-a-secure-random-secret-key-at-least-32-chars!
JWT_ISSUER=somiq-server
JWT_AUDIENCE=somiq-app
JWT_SIGNUP_AUDIENCE=somiq-signup
JWT_AUTH_NAME=auth-jwt
JWT_ACCESS_TOKEN_LIFETIME=1h
JWT_REFRESH_TOKEN_LIFETIME=7d
JWT_SIGNUP_TOKEN_LIFETIME=30m

# Database Configuration
# Local Development (H2 file-based DB):
DB_DRIVER=org.h2.Driver
DB_URL=jdbc:h2:file:./build/db;DB_CLOSE_DELAY=-1
DB_USER=root
DB_PASSWORD=
DB_MAX_POOL_SIZE=10

# Production (PostgreSQL):
# DB_DRIVER=org.postgresql.Driver
# DB_URL=jdbc:postgresql://<db_host>:5432/<db_name>
# DB_USER=<db_user>
# DB_PASSWORD=<db_password>
# DB_MAX_POOL_SIZE=20

# Storage Configuration (LOCAL or GCS)
STORAGE_PROVIDER=LOCAL
GCS_BUCKET_NAME=somiq-uploads
GCS_PUBLIC_URL_PREFIX=http://localhost:8080/media
# GCS_PROJECT_ID=my-gcp-project
# GCS_CREDENTIALS_PATH=/path/to/gcp-credentials.json
# GCS_CREDENTIALS_JSON={"type":"service_account",...}

# CORS Configuration (Comma-separated allowed domains / hosts)
ALLOWED_CORS_HOSTS=localhost,127.0.0.1,10.0.2.2
